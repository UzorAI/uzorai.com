/**
 * Superseded by NavMenu (EPIC #29 Phase C): the brand mark and the
 * ThemeToggle + LanguagePicker controls that lived here are folded into the
 * responsive NavMenu bar. This thin re-export keeps any lingering
 * `import Header from './components/Header'` resolving during the transition.
 *
 * @deprecated import NavMenu instead.
 */
export { default } from './NavMenu'
